main{
    var int a;
    var int b;
    var bool c;
    var float d;
    var void e;
    
    a := 9;
    b := 1;
    c := true;

    a := (a+b);
    b := (a-b);

    a := (a*b);
    a := (a/10);

    return f(a, b);
}

function void f(int a, int b){

    while((c == true))do{
        a := (a-b);
        if((1 > a)){
            c := false;
        };
    };

    print(a);
    return a;
}
